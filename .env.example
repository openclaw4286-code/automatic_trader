# Gate.io futures API (USDT-settled perpetual)
GATE_API_KEY=
GATE_API_SECRET=

# News providers (optional — bot falls back to free sources if empty)
CRYPTOPANIC_TOKEN=

# Claude CLI binary (default: claude)
CLAUDE_CLI=claude

# Runtime
DRY_RUN=true
LOG_LEVEL=INFO
